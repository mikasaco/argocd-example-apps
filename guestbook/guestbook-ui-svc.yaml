apiVersion: v1
kind: Service
metadata:
  name: guestbook-stable
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: guestbook-ui
---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-canary
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: guestbook-ui
